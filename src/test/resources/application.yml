spring:
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    database: h2

  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8081

feign:
  okhttp:
    enabled: true
